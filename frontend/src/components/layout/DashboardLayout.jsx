"use client";

// Compatibility wrapper for legacy pages.
// The actual dashboard shell (sidebar/header) is rendered in app/(dashboard)/layout.js.
export default function DashboardLayout({ children }) {
  return children;
}
